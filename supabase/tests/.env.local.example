# Populate these once per machine (published Supabase CLI defaults, but we don't
# commit them — GitHub's secret scanner treats the pattern as a leak).
# Run `npx supabase status` to see the current values.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
