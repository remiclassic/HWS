# Template for the production admin web app build. Copy to `.env.production` (git-ignored).
# Vite inlines anything prefixed VITE_ into the browser bundle at build time.
#
# ONLY "publishable" / "anon" keys here. The service-role key is server-side only.

VITE_SUPABASE_URL=https://gvlnnjcdqpxqyouwypyq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_IDL4VIURGr0sOZwpHXVZJw_WeI6xm0N
